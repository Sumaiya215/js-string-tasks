let text ="fcgxaveXeam";

    if(text.includes('x') || text.includes('X')){
        var replacedText = text.replace('x', 'y').replace('X', 'Y');
        
    }
    else
    console.log("String does not contain x or X")

console.log(replacedText);